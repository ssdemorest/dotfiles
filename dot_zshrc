alias update-all='ujust update && distrobox enter arch-apps -- "yay -Syu --noconfirm" && flatpak update -y'
alias la='ls -a'
alias arch-run='distrobox enter Arch --no-tty --'
alias grep=rg
# Hello World!


# filen-cli
PATH=$PATH:~/.filen-cli/bin
